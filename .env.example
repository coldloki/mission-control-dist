# Mission Control — Environment Variables

# Port to run on (default: 3001)
PORT=3001

# Path to the SQLite database file
# Use an absolute path if you want it persistent across directories
MEMORY_DB_PATH=./data/tasks.db
